name: TEMP accelerate 21vek v0.7

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/_tmp-accelerate-21vek-v07.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Patch bounded parallel collectors
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          # ---------- card collector ----------
          p=Path('tools/triovist_21vek_shadow_sync.py')
          s=p.read_text(encoding='utf-8')
          s=s.replace('fetches canonical public 21vek product pages sequentially;', 'fetches canonical public 21vek product pages with bounded parallelism;')
          s=s.replace('import hashlib\nimport os\nimport time\n', 'import hashlib\nimport os\nimport threading\nimport time\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\n')
          s=s.replace('DELAY=max(0.4,float(os.environ.get("SHADOW_DELAY_SECONDS","0.8")))\nTIMEOUT=max(10,int(os.environ.get("SHADOW_HTTP_TIMEOUT","25")))', 'DELAY=max(0.05,float(os.environ.get("SHADOW_DELAY_SECONDS","0.14")))\nWORKERS=max(1,min(24,int(os.environ.get("SHADOW_WORKERS","14"))))\nTIMEOUT=max(10,int(os.environ.get("SHADOW_HTTP_TIMEOUT","25")))')
          s=s.replace('PARSER_VERSION="shadow-v0.6"\nUA="ResantaCRM-21vekShadow/0.6 (+https://resanta-crm.by)"', 'PARSER_VERSION="shadow-v0.7"\nUA="ResantaCRM-21vekShadow/0.7 (+https://resanta-crm.by)"')

          start=s.index('def fetch_product(session: requests.Session, url: str) -> requests.Response:')
          end=s.index('\n\nif __name__=="__main__":', start)
          tail=r'''_thread_state=threading.local()
_rate_lock=threading.Lock()
_next_request_at=0.0


def worker_session() -> requests.Session:
    ses=getattr(_thread_state,"session",None)
    if ses is None:
        ses=requests.Session()
        ses.headers.update({
            "User-Agent":UA,
            "Accept":"text/html,application/xhtml+xml",
            "Accept-Language":"ru-RU,ru;q=0.9"
        })
        _thread_state.session=ses
    return ses


def wait_rate_slot() -> None:
    """Globally pace request starts while allowing page downloads/parsing in parallel."""
    global _next_request_at
    with _rate_lock:
        now=time.monotonic()
        slot=max(now,_next_request_at)
        _next_request_at=slot+DELAY
    wait=slot-now
    if wait>0:
        time.sleep(wait)


def fetch_product(session: requests.Session, url: str) -> requests.Response:
    """Fetch one 21vek page with retries for transient network failures."""
    last_error: Exception|None=None
    for attempt in range(3):
        wait_rate_slot()
        try:
            response=session.get(url,timeout=TIMEOUT,allow_redirects=True)
            if response.status_code==200:
                return response
            error=RuntimeError(f"HTTP {response.status_code}")
            if response.status_code<500 and response.status_code not in (408,425,429):
                raise error
            last_error=error
        except (requests.Timeout,requests.ConnectionError) as exc:
            last_error=exc
        if attempt<2:
            time.sleep(1.0*(attempt+1))
    if last_error is not None:
        raise last_error
    raise RuntimeError("21vek fetch failed")


def process_target(index: int, target: dict) -> tuple[int,dict,dict|None,str|None]:
    parsed=None
    err=None
    try:
        r=fetch_product(worker_session(),target["product_url"])
        parsed=parse_product(r.text,{
            "url":target["product_url"],
            "sku":target.get("sku"),
            "donor_article":target.get("donor_article"),
            "name":target.get("product_name"),
            "category":target.get("category"),
            "subgroup":target.get("subgroup")
        })
    except Exception as exc:
        err=str(exc)
    return index,target,parsed,err


def flush_batches(run_id: str, batch: list[dict], raw_batch: list[dict]) -> None:
    if batch:
        rest_post("triovist_21vek_shadow_snapshots",batch,timeout=90)
    if raw_batch:
        rest_upsert(
            "triovist_21vek_shadow_raw_current",
            raw_batch,
            "manager_email,card_key",
            timeout=90
        )


def main() -> None:
    targets=current_targets()
    if recent_complete_matches(len(targets)):
        print(f"Shadow skip: fresh complete {PARSER_VERSION} run already covers {len(targets)} targets",flush=True)
        return
    run_id=insert_run(len(targets))
    print(
        f"Shadow run {run_id}: targets={len(targets)} parser={PARSER_VERSION} "
        f"workers={WORKERS} start_interval={DELAY:.2f}s",
        flush=True
    )

    success=0
    errors=0
    completed=0
    batch=[]
    raw_batch=[]

    try:
        with ThreadPoolExecutor(max_workers=min(WORKERS,max(1,len(targets))),thread_name_prefix="21vek-card") as pool:
            futures=[pool.submit(process_target,idx,target) for idx,target in enumerate(targets,1)]
            for future in as_completed(futures):
                idx,target,parsed,err=future.result()
                completed+=1
                if err is None:
                    success+=1
                else:
                    errors+=1

                batch.append(snapshot_row(run_id,target,parsed,err))
                if parsed is not None:
                    raw_batch.append(raw_current_row(run_id,target,parsed))

                print(
                    f"[{completed}/{len(targets)}] source#{idx} {target.get('sku')} ok={err is None} {err or ''}",
                    flush=True
                )

                if len(batch)>=75:
                    flush_batches(run_id,batch,raw_batch)
                    batch=[]
                    raw_batch=[]

        flush_batches(run_id,batch,raw_batch)

        status="complete" if errors==0 else ("partial" if success else "failed")
        rest_patch("triovist_21vek_shadow_runs",{"id":f"eq.{run_id}"},{
          "status":status,
          "success_count":success,
          "error_count":errors,
          "finished_at":datetime.now(timezone.utc).isoformat(),
          "notes":{
            "mode":"shadow",
            "source_registry":"current triovist_content_cards",
            "offset":OFFSET,
            "production_cutover":False,
            "request_start_interval_seconds":DELAY,
            "workers":WORKERS,
            "approved_scope_version":"2026-09-09-v1",
            "result":"No working CRM tables were modified."
          }
        })
        print(f"Shadow {status}: success={success} errors={errors}",flush=True)
        if status!="complete":
            raise RuntimeError(f"Shadow completeness check failed: success={success} errors={errors}")

    except Exception as exc:
        try:
            rest_patch("triovist_21vek_shadow_runs",{"id":f"eq.{run_id}"},{
              "status":"failed",
              "success_count":success,
              "error_count":max(errors,1),
              "finished_at":datetime.now(timezone.utc).isoformat(),
              "notes":{
                "fatal_error":str(exc)[:1500],
                "workers":WORKERS,
                "request_start_interval_seconds":DELAY,
                "production_cutover":False,
                "approved_scope_version":"2026-09-09-v1"
              }
            })
        except Exception:
            pass
        raise
'''
          s=s[:start]+tail+s[end:]
          p.write_text(s,encoding='utf-8')

          # ---------- TOP collector ----------
          p=Path('tools/triovist_21vek_top_shadow.py')
          s=p.read_text(encoding='utf-8')
          s=s.replace('import hashlib\nimport math\nimport os\nimport time\n', 'import hashlib\nimport math\nimport os\nimport threading\nimport time\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\n')
          s=s.replace("DELAY=max(0.8,float(os.environ.get('TOP_DELAY_SECONDS','1.2')))\nTIMEOUT=max(10,int(os.environ.get('TOP_HTTP_TIMEOUT','30')))", "DELAY=max(0.05,float(os.environ.get('TOP_DELAY_SECONDS','0.18')))\nWORKERS=max(1,min(16,int(os.environ.get('TOP_WORKERS','10'))))\nTIMEOUT=max(10,int(os.environ.get('TOP_HTTP_TIMEOUT','30')))")
          s=s.replace("PARSER_VERSION='top-shadow-v0.2'", "PARSER_VERSION='top-shadow-v0.3'")
          s=s.replace("UA='ResantaCRM-21vekTopShadow/0.2 (+https://resanta-crm.by)'", "UA='ResantaCRM-21vekTopShadow/0.3 (+https://resanta-crm.by)'")

          start=s.index('def wait_delay(last_request_at: float|None) -> None:')
          end=s.index('\n\ndef page_positions(', start)
          search_block=r'''_thread_state=threading.local()
_rate_lock=threading.Lock()
_next_request_at=0.0


def top_session() -> requests.Session:
    ses=getattr(_thread_state,'session',None)
    if ses is None:
        ses=requests.Session()
        ses.headers.update({
          'User-Agent':UA,'Accept':'application/json, text/plain, */*','Content-Type':'application/json',
          'Origin':'https://www.21vek.by','Referer':'https://www.21vek.by/'
        })
        _thread_state.session=ses
    return ses


def wait_delay() -> None:
    global _next_request_at
    with _rate_lock:
        now=time.monotonic()
        slot=max(now,_next_request_at)
        _next_request_at=slot+DELAY
    wait=slot-now
    if wait>0:
        time.sleep(wait)


def search_page(session: requests.Session, keyword: str, page: int, search_id: str) -> dict:
    body=search_body(keyword,page,search_id)
    last_error=None
    for attempt in range(3):
        wait_delay()
        try:
            r=session.post(ENDPOINT,json=body,timeout=TIMEOUT,allow_redirects=True)
            if r.status_code==200:
                try:
                    data=r.json()
                except Exception as exc:
                    raise RuntimeError('21vek search returned non-JSON') from exc
                if not isinstance(data,dict) or not isinstance(data.get('products'),list):
                    raise RuntimeError('21vek search response has no products array')
                return data
            error=RuntimeError(f'21vek search HTTP {r.status_code}: {r.text[:300]}')
            if r.status_code<500 and r.status_code not in (408,425,429):
                raise error
            last_error=error
            retry=r.headers.get('Retry-After') if r.status_code==429 else None
            if retry:
                try:
                    time.sleep(max(1.0,min(30.0,float(retry))))
                except Exception:
                    time.sleep(2.0*(attempt+1))
            elif attempt<2:
                time.sleep(1.0*(attempt+1))
        except (requests.Timeout,requests.ConnectionError) as exc:
            last_error=exc
            if attempt<2:
                time.sleep(1.0*(attempt+1))
    if last_error is not None:
        raise last_error
    raise RuntimeError('21vek search failed')
'''
          s=s[:start]+search_block+s[end:]

          start=s.index('def main() -> None:')
          end=s.index("\n\nif __name__=='__main__':", start)
          main_block=r'''def collect_keyword(run_id: str, keyword: str, targets: list[dict]) -> dict:
    observed=datetime.now(timezone.utc).isoformat()
    by_donor: dict[int,list[dict]]=defaultdict(list)
    for x in targets:
        by_donor[int(x['donor_article'])].append(x)
    found: dict[int,int]={}
    total=None
    search_id=''
    keyword_error=None
    request_count=0
    try:
        ses=top_session()
        data=search_page(ses,keyword,1,'')
        request_count+=1
        search_id=str(data.get('searchId') or '')
        total=int(data.get('total')) if data.get('total') is not None else None
        found.update({pid:pos for pid,pos in page_positions(data.get('products') or [],1).items() if pid in by_donor})

        unresolved=[x for pid,rows in by_donor.items() if pid not in found for x in rows]
        if DEEP_EXACT and unresolved and MAX_DEEP_PAGES>0 and total:
            total_pages=max(1,int(math.ceil(total/60.0)))
            for page in deep_candidates(unresolved,total_pages):
                data2=search_page(ses,keyword,page,search_id)
                request_count+=1
                search_id=str(data2.get('searchId') or search_id)
                pp=page_positions(data2.get('products') or [],page)
                for pid,pos in pp.items():
                    if pid in by_donor:
                        found[pid]=pos
                unresolved=[x for pid,rows in by_donor.items() if pid not in found for x in rows]
                if not unresolved:
                    break
    except Exception as exc:
        keyword_error=str(exc)

    snapshots=[]
    current=[]
    exact_rows=0
    for pid,rows in by_donor.items():
        for x in rows:
            if keyword_error:
                row=row_for(run_id,x,position=None,exact=False,top30=None,top60=None,total=total,page_found=None,observed=observed,error=keyword_error)
            elif pid in found:
                pos=found[pid]
                exact_rows+=1
                row=row_for(run_id,x,position=pos,exact=True,top30=pos<=30,top60=pos<=60,total=total,page_found=int(math.ceil(pos/60.0)),observed=observed,error=None)
            else:
                row=row_for(run_id,x,position=None,exact=False,top30=False,top60=False,total=total,page_found=None,observed=observed,error=None)
            snapshots.append(row)
            current.append(current_row(row))
    return {
      'keyword':keyword,'targets':len(targets),'snapshots':snapshots,'current':current,
      'request_count':request_count,'keyword_error':keyword_error,'exact_rows':exact_rows,'total':total
    }


def main() -> None:
    all_targets=current_targets()
    groups=grouped_targets(all_targets)
    selected=[x for _,xs in groups for x in xs]
    run_id=insert_run(len(groups),len(selected))
    print(
        f'TOP shadow {run_id}: keywords={len(groups)} targets={len(selected)} '
        f'workers={WORKERS} start_interval={DELAY:.2f}s',flush=True
    )

    requests_count=0
    errors=0
    snapshots=[]
    current=[]
    completed=0
    try:
        with ThreadPoolExecutor(max_workers=min(WORKERS,max(1,len(groups))),thread_name_prefix='21vek-top') as pool:
            futures=[pool.submit(collect_keyword,run_id,keyword,targets) for keyword,targets in groups]
            for future in as_completed(futures):
                result=future.result()
                completed+=1
                requests_count+=int(result['request_count'])
                if result['keyword_error']:
                    errors+=1
                snapshots.extend(result['snapshots'])
                current.extend(result['current'])
                print(
                    f"[{completed}/{len(groups)}] {result['keyword']!r}: targets={result['targets']} "
                    f"exact_rows={result['exact_rows']} total={result['total']} error={result['keyword_error'] or '-'}",
                    flush=True
                )
                if len(snapshots)>=250:
                    rest_post('triovist_21vek_top_snapshots',snapshots,timeout=90)
                    rest_upsert('triovist_21vek_top_current',current,'manager_email,card_key',timeout=90)
                    snapshots=[]
                    current=[]

        if snapshots:
            rest_post('triovist_21vek_top_snapshots',snapshots,timeout=90)
            rest_upsert('triovist_21vek_top_current',current,'manager_email,card_key',timeout=90)

        stats=rest_get_all('triovist_21vek_top_snapshots',{
          'run_id':f'eq.{run_id}','select':'position_exact,top30,top60,error_text'
        },page_size=900,timeout=90)
        found_exact=sum(1 for x in stats if x.get('position_exact'))
        top30=sum(1 for x in stats if x.get('top30') is True)
        top60=sum(1 for x in stats if x.get('top60') is True)
        row_errors=sum(1 for x in stats if x.get('error_text'))
        status='complete' if errors==0 and row_errors==0 and len(stats)==len(selected) else 'partial'
        rest_patch('triovist_21vek_top_runs',{'id':f'eq.{run_id}'},{
          'status':status,'requests_count':requests_count,'found_exact_count':found_exact,'top30_count':top30,
          'top60_count':top60,'error_count':errors+row_errors,'finished_at':datetime.now(timezone.utc).isoformat(),
          'notes':{'mode':'shadow','endpoint':'search-composer/api/v3/products','page_size':60,'production_cutover':False,
                   'expected_rows':len(selected),'stored_rows':len(stats),'deep_exact':DEEP_EXACT,
                   'workers':WORKERS,'request_start_interval_seconds':DELAY}
        })
        print(f'DONE {status}: requests={requests_count} rows={len(stats)}/{len(selected)} exact={found_exact} top30={top30} top60={top60} errors={errors+row_errors}',flush=True)
        if status!='complete':
            raise RuntimeError('TOP shadow completeness check failed')
    except Exception as fatal:
        try:
            rest_patch('triovist_21vek_top_runs',{'id':f'eq.{run_id}'},{
              'status':'failed','requests_count':requests_count,'error_count':max(errors,1),
              'finished_at':datetime.now(timezone.utc).isoformat(),
              'notes':{'fatal_error':str(fatal),'production_cutover':False,'workers':WORKERS,'request_start_interval_seconds':DELAY}
            })
        finally:
            raise
'''
          s=s[:start]+main_block+s[end:]
          p.write_text(s,encoding='utf-8')

          # ---------- workflow tuning ----------
          p=Path('.github/workflows/triovist-21vek-shadow-sync.yml')
          s=p.read_text(encoding='utf-8')
          s=s.replace("SHADOW_DELAY_SECONDS: '0.8'", "SHADOW_DELAY_SECONDS: '0.14'\n          SHADOW_WORKERS: '14'")
          s=s.replace("TOP_DELAY_SECONDS='1.2'", "TOP_DELAY_SECONDS='0.18' \\\n          TOP_WORKERS='10'")
          p.write_text(s,encoding='utf-8')
          PY
      - name: Validate Python and workflow snippets
        run: |
          python -m py_compile tools/triovist_21vek_shadow_sync.py tools/triovist_21vek_top_shadow.py
          grep -q "SHADOW_WORKERS: '14'" .github/workflows/triovist-21vek-shadow-sync.yml
          grep -q "TOP_WORKERS='10'" .github/workflows/triovist-21vek-shadow-sync.yml
      - name: Commit accelerated collectors
        run: |
          rm -f .github/workflows/_tmp-accelerate-21vek-v07.yml
          git config user.name 'resanta-crm'
          git config user.email 'resantavitebsc@gmail.com'
          git add tools/triovist_21vek_shadow_sync.py tools/triovist_21vek_top_shadow.py .github/workflows/triovist-21vek-shadow-sync.yml .github/workflows/_tmp-accelerate-21vek-v07.yml
          git commit -m 'perf(21vek): parallelize card and TOP collectors'
          git push
