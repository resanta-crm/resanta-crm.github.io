name: Build Triovist seasonality profile — disabled public builder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Public seasonality builder is disabled for security."
          echo "Triovist seasonality is stored in authenticated Supabase now."
