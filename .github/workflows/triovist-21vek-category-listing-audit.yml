name: Triovist — 21vek category listing audit

on:
  push:
    branches:
      - triovist-21vek-parser-prototype
    paths:
      - 'tools/triovist_21vek_category_listing_audit.py'
      - '.github/workflows/triovist-21vek-category-listing-audit.yml'
  workflow_dispatch:

concurrency:
  group: triovist-21vek-category-listing-audit
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4
      - run: python tools/triovist_21vek_category_listing_audit.py 21vek-category-listing-audit.json
      - uses: actions/upload-artifact@v4
        with:
          name: triovist-21vek-category-listing-audit
          path: 21vek-category-listing-audit.json
          retention-days: 3
